<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>810 enterPrise - criar</title>

  <link rel='stylesheet' href='./stylesheets/style.css' />
<link rel='stylesheet' href='../stylesheets/productToEdit.css' />
</head>
  <body>
      <div class="formulario">
      <h2>Produto a editar:</h2>        
      
      <form action="/productToEdit" method="POST" enctype="multipart/form-data">
        <div>
          <label for="nome">Nome:</label>
          <input type="text" id="nome" name="nome" />
        </div>

        <div>
          <label for="preco">Preço:</label>
          <input type="number" id="preco" name="preco" />
        </div>

        <div>
          <label for="categoria">Categoria:</label>
          <input type="text" id="categoria" name="categoria">
        </div>

        <div>
          <label for="descricao">Descrição:</label>
          <textarea name="descricao" id="descricao"></textarea>
        </div>

        <div>
          <label for="imagem">Imagem do produto:</label>
          <input type="file" name="imagem" id="imagem" />
        </div>

      
              
        <button type="submit">Atualizar produto</button></a>
      </form>
  <% if(typeof createProductError !== 'undefined') { %>
          <ul class="alert alert-danger"
          role="alert"><%= createProductError %></ul>
        <% } %>

        <% if(typeof errors !== 'undefined') { %>
          
            <% errors.forEach(error => { %>
              <ul class="alert alert-danger"
              role="alert"><%= error.message %>
              <% }) %>
            </ul>
            <% } %>
          
        </div>
  </body>
</html>
