const compraFinalizadaController = {
    renderCompraFinalizada: (req, res) => {
        res.render('compraFinalizada', { title: 'compra finalizada' });
        }
    }

    module.exports = compraFinalizadaController;